package knightage.login
{
	import jsion.core.modules.BaseModule;
	import jsion.core.modules.ModuleInfo;
	
	public class KLoginModule extends BaseModule
	{
		public function KLoginModule(info:ModuleInfo)
		{
			super(info);
		}
	}
}