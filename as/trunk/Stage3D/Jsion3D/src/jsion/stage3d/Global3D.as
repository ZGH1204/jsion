package jsion.stage3d
{
	/**
	 * AGAL简明教程：http://www.adobe.com/content/dotcom/cn/devnet/flashplayer/articles/what-is-agal.html
	 * @author Jsion
	 * 
	 */	
	public class Global3D
	{
		/**
		 * 属性寄存器0(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister0:String = "va0";
		
		/**
		 * 属性寄存器1(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister1:String = "va1";
		
		/**
		 * 属性寄存器2(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister2:String = "va2";
		
		/**
		 * 属性寄存器3(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister3:String = "va3";
		
		/**
		 * 属性寄存器4(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister4:String = "va4";
		
		
		/**
		 * 属性寄存器5(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister5:String = "va5";
		
		/**
		 * 属性寄存器6(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister6:String = "va6";
		
		/**
		 * 属性寄存器7(共8个)
		 * 这些寄存器引用作为顶点着色器输入的VertexBuffer的顶点属性数据
		 */		
		public static const VertexPropRegister7:String = "va7";
		
		/**
		 * 顶点着色器常量寄存器0(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister0:String = "vc0";
		
		/**
		 * 顶点着色器常量寄存器1(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister1:String = "vc1";
		
		/**
		 * 顶点着色器常量寄存器2(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister2:String = "vc2";
		
		/**
		 * 顶点着色器常量寄存器3(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister3:String = "vc3";
		
		/**
		 * 顶点着色器常量寄存器4(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister4:String = "vc4";
		
		/**
		 * 顶点着色器常量寄存器5(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister5:String = "vc5";
		
		/**
		 * 顶点着色器常量寄存器6(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister6:String = "vc6";
		
		/**
		 * 顶点着色器常量寄存器7(共有128个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const VertexConstRegister7:String = "vc7";
		
		/**
		 * 像素着色器常量寄存器0(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister0:String = "fc0";
		
		/**
		 * 像素着色器常量寄存器1(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister1:String = "fc1";
		
		/**
		 * 像素着色器常量寄存器2(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister2:String = "fc2";
		
		/**
		 * 像素着色器常量寄存器3(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister3:String = "fc3";
		
		/**
		 * 像素着色器常量寄存器4(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister4:String = "fc4";
		
		/**
		 * 像素着色器常量寄存器5(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister5:String = "fc5";
		
		/**
		 * 像素着色器常量寄存器6(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister6:String = "fc6";
		
		/**
		 * 像素着色器常量寄存器7(共28个)
		 * 这些寄存器用于处理从ActionScript传递到着色器的参数。这由 Context3D::setProgramConstants() 系列函数执行。
		 */		
		public static const PixelConstRegister7:String = "fc7";
		
		/**
		 * 顶点着色器临时寄存器0(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister0:String = "vt0";
		
		/**
		 * 顶点着色器临时寄存器1(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister1:String = "vt1";
		
		/**
		 * 顶点着色器临时寄存器2(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister2:String = "vt2";
		
		/**
		 * 顶点着色器临时寄存器3(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister3:String = "vt3";
		
		/**
		 * 顶点着色器临时寄存器4(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister4:String = "vt4";
		
		/**
		 * 顶点着色器临时寄存器5(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister5:String = "vt5";
		
		/**
		 * 顶点着色器临时寄存器6(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister6:String = "vt6";
		
		/**
		 * 顶点着色器临时寄存器7(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const VertexTempRegister7:String = "vt7";
		
		
		/**
		 * 像素着色器临时寄存器0(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister0:String = "ft0";
		
		
		/**
		 * 像素着色器临时寄存器1(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister1:String = "ft1";
		
		
		/**
		 * 像素着色器临时寄存器2(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister2:String = "ft2";
		
		
		/**
		 * 像素着色器临时寄存器3(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister3:String = "ft3";
		
		
		/**
		 * 像素着色器临时寄存器4(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister4:String = "ft4";
		
		
		/**
		 * 像素着色器临时寄存器5(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister5:String = "ft5";
		
		
		/**
		 * 像素着色器临时寄存器6(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister6:String = "ft6";
		
		
		/**
		 * 像素着色器临时寄存器7(共8个)
		 * 这些寄存器可用于着色器，它们用于临时计算。因为AGAL不使用变量，您将使用临时寄存器来存储整个代码中的数据。
		 */		
		public static const PixelTempRegister7:String = "ft7";
		
		
		/**
		 * 顶点着色器输出寄存器
		 * 输出寄存器由顶点和像素寄存器用于存储它们的计算的输出。对于顶点着色器，此输出是顶点的位置。对于像素着色器，它是像素的颜色。
		 */		
		public static const VertexOutputRegister:String = "op";
		
		/**
		 * 像素着色器输出寄存器
		 * 输出寄存器由顶点和像素寄存器用于存储它们的计算的输出。对于顶点着色器，此输出是顶点的位置。对于像素着色器，它是像素的颜色。
		 */		
		public static const PixelOutputRegister:String = "oc";
		
		/**
		 * 可变寄存器0(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister0:String = "v0";
		
		/**
		 * 可变寄存器1(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister1:String = "v1";
		
		/**
		 * 可变寄存器2(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister2:String = "v2";
		
		/**
		 * 可变寄存器3(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister3:String = "v3";
		
		/**
		 * 可变寄存器4(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister4:String = "v4";
		
		/**
		 * 可变寄存器5(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister5:String = "v5";
		
		/**
		 * 可变寄存器6(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister6:String = "v6";
		
		/**
		 * 可变寄存器7(共8个)
		 * 这些寄存器用于将数据从顶点着色器传递到像素着色器。传递的数据由GPU恰当地插入，使像素着色器能够收到被处理的像素的正确值。
		 * 以这种方式传入的典型数据是顶点颜色或纹理的UV坐标。
		 */		
		public static const VariableRegister7:String = "v7";
	}
}